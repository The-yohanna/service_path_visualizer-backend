import { Document, Model } from 'mongoose';
import { MongooseIdAssigner, NormalisedOptions } from '../MongooseIdAssigner';
interface OptionsCheckResults {
    abort?: boolean;
    replace?: boolean;
    delete?: boolean;
    options: NormalisedOptions;
}
export declare function checkAndUpdateOptions(options: NormalisedOptions, freshOptions?: NormalisedOptions): OptionsCheckResults;
export declare function initialiseOptions(mongooseModel: Model<Document>, idAssigner: MongooseIdAssigner, retries?: number): Promise<number>;
export {};
